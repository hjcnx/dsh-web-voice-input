# Offline test overlay for dsh-voice-input.
# Use with: dsh web --port <port> --patch <this file>
# The route then answers with this canned text and never calls the upstream
# ASR API — for verifying the browser pipeline without a working API key.
- id: voice-input
  config:
    mock: '这是一段语音识别测试文本。'
